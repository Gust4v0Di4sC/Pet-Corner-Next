// Placeholder for the next cycle: Firestore customer profile adapter.
export type FirebaseCustomerAdapter = {
  provider: "firestore";
  collection: "customers";
};

export const firebaseCustomerAdapter: FirebaseCustomerAdapter = {
  provider: "firestore",
  collection: "customers",
};
