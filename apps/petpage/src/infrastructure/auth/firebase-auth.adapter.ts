// Placeholder for the next cycle: Firebase Auth adapter implementation.
export type FirebaseAuthAdapter = {
  provider: "firebase-auth";
};

export const firebaseAuthAdapter: FirebaseAuthAdapter = {
  provider: "firebase-auth",
};
